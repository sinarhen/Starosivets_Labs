#include <iostream>

using namespace std;

// function to check if input value is numeric
bool isNumeric(const std::string &str);

// function to check if input value is more than one
bool isMoreThanOne(int val);
